'use strict';

const maDesignUtils = require('..');
const assert = require('assert').strict;

assert.strictEqual(maDesignUtils(), 'Hello from maDesignUtils');
console.info('maDesignUtils tests passed');
